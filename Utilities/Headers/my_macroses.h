
#ifndef REINCLUDE_PROTECTION_MY_MACROSES
#define REINCLUDE_PROTECTION_MY_MACROSES

#define Min(a, b) (((a) < (b)) ? (a) : (b))
#define Max(a, b) (((a) > (b)) ? (a) : (b))
#define Is_number(ch) (((ch) >= '0') && ((ch) <= '9'))
#define PERCENT_OF(n1, n2) (((n1) * 100) / (n2))

#endif //!REINCLUDE_PROTECTION_MY_MACROSES